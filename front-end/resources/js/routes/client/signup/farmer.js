(function() {
  'use strict';

  angular
    .module('app.signup')
    .controller('Farmer', Farmer);

    Farmer.$inject = ['$rootScope'];

    /* @ngInject */
    function Farmer($rootScope) {
      var vm = this;

    }
}());
